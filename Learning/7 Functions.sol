pragma solidity 0.8.11;

contract ZombieFactory {

    function eatHamburgers(string memory _name, uint _amount ) public{
        // 1 memory is required for all reference types such as arrays, structs, mappings, and strings.
        // 2. parameter variable names with an underscore (_)
    }

}